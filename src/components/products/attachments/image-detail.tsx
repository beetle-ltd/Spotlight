import { Attachment } from "@/models/Attachment";

type TImageDetailProps = {
  attachments: Attachment[];
};
const ImageDetail = ({ attachments }: TImageDetailProps) => {
  return <img src="" alt="" />;
};

export default ImageDetail;
